import { AnyAction } from 'redux';

const filtersReducer = (state = {}, action: AnyAction) => {

    switch (action.type) {
        default:
            return state;
    }

};

export { filtersReducer };